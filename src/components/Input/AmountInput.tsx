
// function AmountInput({amountInFromCurrency, amount, handleFromAmountChange, rates, fromCurrency}) {

//   function calculateCurrency(): Number {
//       if (rates[fromCurrency] && rates[toCurrency]) {
//         return amountInFromCurrency ? (amount * rates[toCurrency] / rates[fromCurrency]) : (amount * rates[fromCurrency] / rates[toCurrency]);
//       } else {
//         return 0;
//       }
//     }
      
//   return (
//       <input type="number" value={String(amountInFromCurrency ? amount : calculateCurrency())} onChange={handleFromAmountChange} placeholder='From...' min={0}/>
//   )
// }

// export default AmountInput;
